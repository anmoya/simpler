# Tauri desktop first

The first product surface will be a desktop app built with Tauri. The app needs native access to folders, files, Git operations, and credentials while keeping a web-based UI layer, and a pure web app or CLI/TUI would not fit the desired daily writing workflow.
