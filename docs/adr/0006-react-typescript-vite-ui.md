# React TypeScript Vite UI

The Tauri UI will be built with React, TypeScript, and Vite. This stack fits a component-based editor interface, integrates well with CodeMirror 6, and keeps iteration fast while giving the workspace, command, and sync state strong typing.
