# Sync model over Git UI

The app will present synchronization as a simple product model instead of exposing Git as the primary daily interface. Git remains the storage and transport mechanism, with technical controls available in an advanced view, because the core workflow is writing notes rather than managing commits, branches, pulls, and pushes.
